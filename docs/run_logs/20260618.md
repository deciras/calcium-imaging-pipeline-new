# 20260618 运行日志（补记）

## Pipeline 入口和操作体验整理

今天的工作主要围绕 pipeline 入口、工作站运行方式和 manual 前后流程衔接做整理。该日没有形成正式数据运行日志，本文件为后补记录，用于保留上下文。

重点方向：

- 继续把 Mac 和 Linux workstation 视为同一套 pipeline contract，只允许绝对路径、系统入口和环境位置不同。
- 检查 pipeline dashboard / launcher 相关入口，避免手动运行时需要记忆过多零散命令。
- 继续把 `00_original_files/`、`00_stim_logs_raw/`、`stim_logs/` 的职责分开：前两者分别作为显微镜原始数据和刺激原始日志归档，`stim_logs/` 只作为旧逻辑兼容层。
- 对 manual ROI GUI 和前处理入口的近期变更做晨间简报整理，提醒下一步应做小范围真实数据闭环验证。

## 当日结论

- 这一天主要是 workflow / GUI / launcher 层面的梳理，不记录新的生物学分析结论。
- 后续日志中如果涉及真实数据，应继续区分“代码入口已准备好”和“真实数据已跑通并检查结果”。


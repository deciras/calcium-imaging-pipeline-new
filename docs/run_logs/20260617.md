# 20260617 运行日志

## 统一本地和工作站数据结构

今天把本地说明、工作站说明和总入口的默认路径统一到同一套实验数据布局：

同步原则正式明确为：Linux workstation 和 Mac 需要即刻同步化。除了操作系统差异、绝对路径、Fiji/conda 位置和少量平台入口脚本不同之外，代码逻辑、步骤顺序、数据目录结构、文件命名、参数含义、README 说明和运行日志都应该一致，不维护两套不同规则。

```text
DATA_ROOT/
  00_original_files/
  00_stim_logs_raw/
  01_oir_to_tif/
  02_stim_map/
  ...
```

明确约定：

- `00_original_files/` 只放显微镜原始数据，例如日期/session 文件夹、`.oir` 和同名原始附件。
- `00_stim_logs_raw/` 只放刺激控制程序导出的原始参数合集，例如 `*_motor_rotation/` 里的 `timestamp_log_*.csv`、`stim_map_*.csv`、`experiment_config_*.json` 和 `*_angle_list.txt`。
- `stim_logs/` 如果存在，只是给旧逻辑兼容用的 flat symlink/index，不作为原始刺激参数归档。

当前测试数据集改为：

```text
/Volumes/Yifei_Ding/20260617_test
```

## 代码修改

- `current/run_pipeline.py`
  - 新增默认刺激日志根目录选择逻辑。
  - 未显式传 `--stim-log-root` 时，优先使用 `DATA_ROOT/00_stim_logs_raw`。
  - 如果没有 `00_stim_logs_raw`，再回退到旧的 `DATA_ROOT/stim_logs`。
  - dry-run 验证 step 02 会自动收到：

```text
--stim-log-root /Volumes/Yifei_Ding/20260617_test/00_stim_logs_raw
```

- `linux_workstation/prepare_stim_logs.py`
  - 保持 `00_stim_logs_raw` 为原始刺激记录归档。
  - `stim_logs/` 继续作为 flat index/symlink 输出。
  - 忽略 Mac 资源叉文件 `._*`，避免误当作刺激参数文件。
  - 执行结束后自动清理目标 `stim_logs/` 里的 `._*` 伴生文件。

## 文档修改

- 更新主 `README.md`
  - 示例数据路径改为 `/Volumes/Yifei_Ding/20260617_test`。
  - 文件夹结构加入 `00_stim_logs_raw/`。
  - 补充 `00_original_files/`、`00_stim_logs_raw/` 和 `stim_logs/` 的用途边界。
- 更新 `linux_workstation/README.md`
  - 工作站说明同步为相同的数据结构。
  - 明确 step 02 默认优先读 `00_stim_logs_raw/`。
- 更新 `docs/run_pipeline_command_guide.md`
  - 示例路径改为新测试数据集。
  - 补充当前测试数据集和数据目录约定。
- 顺手更新 `docs/roi_curation_gui.md` 里的旧测试路径。

## 数据整理验证

对新测试数据集执行：

```bash
python3 linux_workstation/prepare_stim_logs.py /Volumes/Yifei_Ding/20260617_test --organize-raw --overwrite --execute
```

结果：

- 原始刺激合集保留在 `/Volumes/Yifei_Ding/20260617_test/00_stim_logs_raw/`
- 兼容索引刷新到 `/Volumes/Yifei_Ding/20260617_test/stim_logs/`
- 有效索引条目为 84 个：83 个真实刺激日志/参数文件加 1 个 `stim_logs_manifest.csv`
- `stim_logs/` 中没有 `._*` 资源叉文件

## 验证命令

```bash
python3 current/run_pipeline.py --steps 02 --data-root /Volumes/Yifei_Ding/20260617_test --dry-run
```

结果：总入口只生成计划，没有启动实际处理；step 02 命令已自动带上 `--stim-log-root /Volumes/Yifei_Ding/20260617_test/00_stim_logs_raw`。

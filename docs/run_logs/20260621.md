# 20260621 运行日志（补记）

## 刺激日志和工作站数据布局

今天的重点是继续维护 `00_stim_logs_raw/` 作为刺激程序原始输出归档，并保持 Mac / Linux workstation 的数据布局一致。

关键约定：

- 原始刺激参数合集进入：

```text
DATA_ROOT/00_stim_logs_raw/
```

- 旧逻辑兼容索引如果存在，仍可由 `linux_workstation/prepare_stim_logs.py` 生成到：

```text
DATA_ROOT/stim_logs/
```

- 不能把 `stim_logs/` 当作唯一真实来源；后续 step 02 应优先从 `00_stim_logs_raw/` 递归发现 `timestamp_log_*.csv`、`experiment_config_*.json`、`stim_map_*.csv` 和 `*_angle_list.txt`。

## 当日结论

- 继续执行 Mac / workstation 立即同步原则。
- 后续如果出现 “no stim / no AoLP” 的报告结果，应先检查 `00_stim_logs_raw/` 中是否存在匹配日志，再判断是否是真无刺激或实验配置问题。


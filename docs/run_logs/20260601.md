# 20260601 运行日志

## 概要

重点：继续优化 05e 人工 ROI 标注 GUI 的可用性和导出格式。

## Progress

### 2026-06-01 05e 标注界面和导出优化

修改内容：

- suite2p 参考 ROI 默认只显示 suite2p 原本 accepted 的 ROI
- 勾选 `show rejected` 时才显示 suite2p rejected ROI
- suite2p 未选中参考 ROI 改为较细的半透明橙色 contour
- suite2p 被选中或正在查看时显示为更醒目的颜色
- freehand / ellipse 当前绘制线条继续调细，更接近 Fiji 手画时的线宽
- 手画完成后的 ROI 会显示在左边 overlay 上，可以像其他候选一样选中、reject、delete
- 右侧控制区增加上下滚动条，屏幕较小时按钮不会被挤出窗口

新增保存格式：

- `suite2p_compatible/plane0/stat.npy`
- `suite2p_compatible/plane0/iscell.npy`
- `suite2p_compatible/plane0/F.npy`
- `suite2p_compatible/plane0/Fneu.npy`
- `RoiSet.zip`

说明：

- `stat.npy` / `iscell.npy` 用于兼容 suite2p 风格的下游读取
- `F.npy` / `Fneu.npy` 只有在 trace 长度一致时导出
- `RoiSet.zip` 用 ImageJ/Fiji polygon ROI 格式保存；freehand 和 ellipse 都以轮廓多边形形式打开

验证：

- `05e_roi_curation_gui.py` 语法检查通过
- offscreen 模式测试保存成功
- 已确认生成：
  - `suite2p_compatible/plane0/stat.npy`
  - `suite2p_compatible/plane0/iscell.npy`
  - `RoiSet.zip`

## Next

- 继续实际试用 05e，检查手感和显示是否舒服
- 后续让 06 读取 05e 的 `manual_roi_set.json` 或 `suite2p_compatible/plane0/`

# 20260707 运行日志

## 概要

- 整理并准备一次面向 public GitHub 仓库的集中更新。
- 将主要 `README` 统一改为中英双语，并在顶部加入语言切换按钮。
- 复核本次准备公开的代码、启动脚本、历史归档说明、流程图与运行日志范围。

## Progress

### README 双语化与 public 入口整理

- 已更新：
  - `README.md`
  - `current/README.md`
  - `envs/README.md`
  - `linux_workstation/README.md`
  - `docs/run_logs/README.md`
  - `archive/current_cleanup_20260623/README.md`
- 新增：
  - `archive/20260603_code/README.md`
- 统一结构：
  - 顶部加入中英文切换按钮；
  - 中文与 English 分段；
  - 将 public 仓库首页的重点放在项目定位、数据结构、运行入口、边界和附加文档链接，而不是只堆运行命令。

### dashboard 文档与跨平台薄启动层补充

- 已更新：
  - `docs/pipeline_dashboard_gui.md`
  - `README.md`
- 已新增：
  - `windows/README.md`
  - `windows/launch_pipeline_dashboard.bat`
  - `windows/launch_pipeline_dashboard.ps1`
- 本次明确写入的原则：
  - macOS、Linux、Windows 不维护三套彼此复制的主代码；
  - `current/` 中的 pipeline 与 dashboard 主逻辑保持单份共享；
  - 各平台只保留很薄的启动脚本、默认路径和可执行文件定位差异。

### 本地重复 README 与 archive 总说明整理

- 已新增：
  - `archive/README.md`
- 已处理：
  - 清理未纳管的 `README 2.md` 重复副本，避免它们继续干扰工作区状态判断。
- 归档层现在更明确：
  - `archive/` 只用于历史追溯、差异比较和清理记录；
  - 日常运行仍然以 `current/` 为准。

### GitHub review 回补修复

- 已修复：
  - `run_pipeline.py`
    - `--date-id` 不再把 step 00 的 `data-root` 错误缩到日期子目录；
    - step 01 改为保留总 `data-root`，并通过新的 `--original-root` 安全指向指定日期原始目录，避免改坏既有原始/输出布局。
  - `current/preprocess/01_fiji_totif_ini.py`
    - 新增 `--original-root`，允许只处理某个 raw 子目录，同时保持输出根目录不变。
  - `current/tools/export_trial_sync_video.py`
    - 当导出速度低于采集帧率时，保留重复帧，不再错误地自动提速。
  - `current/tools/generate_manual_stim_logs_from_notes.py`
    - `onTime/offTime/intialDelayTime` 按毫秒解释后再换算成秒，避免 pulse protocol 被放大 1000 倍。
- 最小验证：
  - `py_compile` 已通过；
  - date-filter dry-run 已确认 step 00 保持完整 `data-root`，step 01 改走 `--original-root`；
  - 毫秒转秒 helper 已确认 `100 -> 0.1 s`、`10000 -> 10.0 s`。

### 本次准备公开的代码与入口

- 纳入本次更新的内容包括：
  - stimulus 兼容路径相关修复；
  - `--date-id` 按日期处理入口；
  - dashboard 启动脚本与 Linux 桌面入口；
  - 手工 stim log 生成工具；
  - 单 movie / 双 movie / raw-analog 同步视频导出工具；
  - 流程图与近期运行日志。

### 语法与发布边界检查

- 使用 `py_compile` 检查了本次变动的 CPython 脚本入口，结果通过。
- `current/preprocess/01_fiji_totif_worker.py` 未纳入 `py_compile` 成功判定：
  - 原因是该脚本面向 Fiji/Jython，包含 `from loci.plugins.in import ...` 这类在 CPython 3 下会触发语法错误的导入写法；
  - 这不是本次改动新引入的问题，而是运行时环境差异。
- 检查了 `.gitignore` 与暂存范围，未发现真实显微原始数据、大型数组或 suite2p 输出被纳入提交。

## Issues / Notes

- `archive/20260603_code/` 是历史代码快照，只应作为对照与追溯用途，不应和 `current/` 主线混用。
- `manual` 仍然是单 trial GUI，不是自动日期批处理 GUI。
- `13` 之后的汇总步骤尚未全部扩展成日期级筛选。

## Next

- 提交并 push 本次 public 更新。
- push 后建议到 GitHub 页面手动检查：
  - 仓库首页 README 的语言切换按钮是否正常；
  - SVG / PPTX 流程图链接是否正常；
  - 新增运行日志与 archive 说明页是否可读。

## Future

- 如后续还要继续面向外部用户优化，可再补：
  - 根目录 `CONTRIBUTING.md`
  - `docs/` 层的双语导航页
  - 针对无刺激数据、早期数据和日期筛选的专门 FAQ

## 今日结论

- 当前仓库已经具备一次较完整的 public 更新条件：
  - 主要代码修复已纳入；
  - README 结构更适合外部阅读；
  - 运行边界与历史归档说明更清楚；
  - 发布前的最小语法检查与暂存范围检查已完成。

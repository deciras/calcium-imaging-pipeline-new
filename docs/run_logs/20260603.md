# 20260603 运行日志

## 概要

重点：05e 人工 ROI 标注 GUI 增加图像来源切换。

## Progress

### 2026-06-03 00:26 05e 支持选择 raw / corrected / high-pass 图像

修改内容：

- `05e_roi_curation_gui.py` 新增 `Movie source` 下拉框
- GUI 内可在三种图像来源之间切换：
  - `raw (01 converted TIFF)`：01 转出的主成像 TIFF，通常是 `*_Max_Proj.tif`
  - `motion corrected (03)`：03 运动矫正后的 `*_corrected_movie.tif`
  - `spatial high-pass (04)`：04 空间高通滤波后的 `*_spatial_highpass_movie.tif`
- 切换图像来源时会重新扫描该来源下的 trial
- 如果当前 trial 在新来源下存在，就继续打开当前 trial
- 如果当前 trial 不存在，就打开新来源下第一个可用 trial
- 切换前如果有未保存 ROI 修改，会提示保存
- `run_pipeline.py --movie-kind` 增加 `raw`

验证：

- `05e_roi_curation_gui.py` 语法检查通过
- `run_pipeline.py` 语法检查通过
- test dataset 中三种来源均识别到 6 个 trial：
  - raw: 6
  - corrected: 6
  - spatial-highpass: 6
- offscreen 模式测试 GUI 内部从 corrected 切到 raw，再切到 spatial-highpass 成功
- `run_pipeline.py --steps 05e --movie-kind raw --dry-run` 通过

## Next

- 继续实际试用 05e 的手感
- 下一步让 06 支持读取 05e 保存的人工 ROI set

### 2026-06-03 00:42 05e 右侧编辑视图同步 ROI

修改内容：

- 左边选中的 suite2p ROI 加入最终 ROI set 后，会同步显示到右边
- 右边窗口现在显示最终会保存的 ROI：
  - accepted 手画 ROI
  - 已选中的 suite2p 参考 ROI
- 右边 select 模式支持点选手画 ROI
- 右边 select 模式支持点选已选 suite2p ROI
- 右键手画 ROI：reject
- 右键已选 suite2p ROI：从最终 ROI set 中移除
- `Delete` / `Backspace` 继续删除当前选中的 ROI

验证：

- `05e_roi_curation_gui.py` 语法检查通过
- offscreen 模式确认：
  - 右边可以选中手画 ROI
  - 右边可以选中已选 suite2p ROI
  - 右键已选 suite2p ROI 可移出最终 ROI set
  - 保存流程不受影响

### 2026-06-03 00:49 05e 右侧手画 ROI 右键删除

修改内容：

- 右边编辑视图中，右键手画 ROI 改为直接删除
- `X` 仍然用于 reject 当前 ROI
- `Ctrl+Z` / `U` 可以撤销右键删除

验证：

- `05e_roi_curation_gui.py` 语法检查通过
- offscreen 模式确认右键手画 ROI 后直接从 ROI 列表删除
- 撤销后手画 ROI 可以恢复

### 2026-06-03 01:05 05e 增加播放/暂停和 FPS 控制

修改内容：

- 05e 增加 `Play/Pause` 按钮
- 增加 `fps` 控制，可以调 movie 播放速度
- 空格键可以播放/暂停
- 播放状态下仍然可以点选、删除、reject 或手画 ROI
- 播放按一帧一帧前进并循环，不主动跳过中间帧；如果电脑忙，会变慢而不是吃帧
- 没有选中 ROI 时，左右键改为逐帧前后移动
- 有 ROI 被选中时，方向键继续用于选择最近的 ROI

验证：

- `05e_roi_curation_gui.py` 语法检查通过
- offscreen 模式确认：
  - GUI 可以启动
  - `Play` 后计时器启动
  - 播放会前进一帧
  - `Pause` 后计时器停止
  - FPS 设置可以生效

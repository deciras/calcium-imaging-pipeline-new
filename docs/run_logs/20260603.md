# 20260603 运行日志

## 概要

重点：05e 人工 ROI 标注 GUI 增加图像来源切换。

## Progress

### 2026-06-03 00:26 05e 支持选择 raw / corrected / high-pass 图像

修改内容：

- `05e_roi_curation_gui.py` 新增 `Movie source` 下拉框
- GUI 内可在三种图像来源之间切换：
  - `raw (01 converted TIFF)`：01 转出的主成像 TIFF，通常是 `*_Max_Proj.tif`
  - `motion corrected (03)`：03 运动矫正后的 `*_corrected_movie.tif`
  - `spatial high-pass (04)`：04 空间高通滤波后的 `*_spatial_highpass_movie.tif`
- 切换图像来源时会重新扫描该来源下的 trial
- 如果当前 trial 在新来源下存在，就继续打开当前 trial
- 如果当前 trial 不存在，就打开新来源下第一个可用 trial
- 切换前如果有未保存 ROI 修改，会提示保存
- `run_pipeline.py --movie-kind` 增加 `raw`

验证：

- `05e_roi_curation_gui.py` 语法检查通过
- `run_pipeline.py` 语法检查通过
- test dataset 中三种来源均识别到 6 个 trial：
  - raw: 6
  - corrected: 6
  - spatial-highpass: 6
- offscreen 模式测试 GUI 内部从 corrected 切到 raw，再切到 spatial-highpass 成功
- `run_pipeline.py --steps 05e --movie-kind raw --dry-run` 通过

## Next

- 继续实际试用 05e 的手感
- 下一步让 06 支持读取 05e 保存的人工 ROI set

### 2026-06-03 00:42 05e 右侧编辑视图同步 ROI

修改内容：

- 左边选中的 suite2p ROI 加入最终 ROI set 后，会同步显示到右边
- 右边窗口现在显示最终会保存的 ROI：
  - accepted 手画 ROI
  - 已选中的 suite2p 参考 ROI
- 右边 select 模式支持点选手画 ROI
- 右边 select 模式支持点选已选 suite2p ROI
- 右键手画 ROI：reject
- 右键已选 suite2p ROI：从最终 ROI set 中移除
- `Delete` / `Backspace` 继续删除当前选中的 ROI

验证：

- `05e_roi_curation_gui.py` 语法检查通过
- offscreen 模式确认：
  - 右边可以选中手画 ROI
  - 右边可以选中已选 suite2p ROI
  - 右键已选 suite2p ROI 可移出最终 ROI set
  - 保存流程不受影响

### 2026-06-03 00:49 05e 右侧手画 ROI 右键删除

修改内容：

- 右边编辑视图中，右键手画 ROI 改为直接删除
- `X` 仍然用于 reject 当前 ROI
- `Ctrl+Z` / `U` 可以撤销右键删除

验证：

- `05e_roi_curation_gui.py` 语法检查通过
- offscreen 模式确认右键手画 ROI 后直接从 ROI 列表删除
- 撤销后手画 ROI 可以恢复

### 2026-06-03 01:05 05e 增加播放/暂停和 FPS 控制

修改内容：

- 05e 增加 `Play/Pause` 按钮
- 增加 `fps` 控制，可以调 movie 播放速度
- 空格键可以播放/暂停
- 播放状态下仍然可以点选、删除、reject 或手画 ROI
- 播放按一帧一帧前进并循环，不主动跳过中间帧；如果电脑忙，会变慢而不是吃帧
- 没有选中 ROI 时，左右键改为逐帧前后移动
- 有 ROI 被选中时，方向键继续用于选择最近的 ROI

验证：

- `05e_roi_curation_gui.py` 语法检查通过
- offscreen 模式确认：
  - GUI 可以启动
  - `Play` 后计时器启动
  - 播放会前进一帧
  - `Pause` 后计时器停止
  - FPS 设置可以生效

### 2026-06-03 02:05 05e 手动拖动时间条时自动暂停

修改内容：

- 播放过程中，如果开始拖动 frame 滑条，会自动暂停
- 这样可以直接进入手动前后看帧的模式
- 程序自己播放更新滑条时不会触发这个暂停逻辑

验证：

- `05e_roi_curation_gui.py` 语法检查通过
- offscreen 模式确认播放状态下触发滑条按下会暂停

### 2026-06-03 02:20 05e 修正右侧手画 ROI 删除和椭圆预览线

修改内容：

- 右边窗口中，手画 ROI 现在可以在任何模式下右键删除
- 不需要先切回 `select ROI` 模式
- 如果右键没有点到已有手画 ROI，freehand 模式仍保留撤销当前绘制线段的行为
- 椭圆绘制预览不再显示一串粗圆点，改成和 freehand 一样的细线

验证：

- `05e_roi_curation_gui.py` 语法检查通过
- offscreen 模式确认在 `draw freehand ROI` 模式下右键已有手画 ROI 会直接删除

### 2026-06-03 02:28 05e 允许手画模式下移除已选 suite2p ROI

修改内容：

- 右边窗口中，已选进最终 ROI set 的 suite2p ROI 也可以在任意手画模式下右键移除
- 右键优先处理已有 ROI；如果没有点到已有 ROI，freehand 模式才撤销当前绘制线段

验证：

- `05e_roi_curation_gui.py` 语法检查通过
- offscreen 模式确认在 `draw freehand ROI` 模式下右键已选 suite2p ROI 会从最终 ROI set 移除

### 2026-06-03 02:36 05e 右键删除不再依赖当前选中状态

修改内容：

- 右键删除/移除改为直接处理鼠标点到的 ROI
- 不需要先把 ROI 设成当前选中
- 手画 ROI 删除和已选 suite2p ROI 移除都走直接命中逻辑
- `Delete` 键仍然保留“删除当前选中 ROI”的行为

验证：

- `05e_roi_curation_gui.py` 语法检查通过
- offscreen 模式确认：
  - 未选中手画 ROI 时，右键点中它可以直接删除
  - 未选中已选 suite2p ROI 时，右键点中它可以直接移除

### 2026-06-03 02:50 05e 支持重新加载已保存校对结果

修改内容：

- 重新打开同一个 trial 时，会自动读取上次保存的 05e 结果
- 会恢复：
  - 已选的 suite2p ROI
  - 已移除的 suite2p 参考 ROI
  - 手画 ROI
- 手画 ROI 读回后会重新计算 trace，方便继续看 `F`、`Fneu`、`F - 0.7Fneu` 和 dF/F
- 可以分多次校对，不需要一次完成

验证：

- `05e_roi_curation_gui.py` 语法检查通过
- offscreen 模式确认保存后重新打开同一个 trial，可以恢复手画 ROI 和已选 suite2p ROI

### 2026-06-03 03:05 05e 切换 Movie source 不再要求保存

修改内容：

- `raw`、`motion corrected`、`spatial high-pass` 明确作为同一 trial 的不同查看底片
- 当前 trial 在新 movie source 下存在时，切换 source 不再提示保存
- 切换 source 时保留当前手画 ROI、已选 suite2p ROI、删除状态和 dirty 状态
- 手画 ROI 的 trace 会按新 movie 重新计算
- 如果新 source 没有当前 trial、需要切到另一个 trial，仍然会提示保存未保存修改

验证：

- `05e_roi_curation_gui.py` 语法检查通过
- offscreen 模式确认 dirty 状态下从 corrected 切到 spatial-highpass 不调用保存提示
- offscreen 模式确认切换后手画 ROI 仍保留

### 2026-06-03 03:25 05e 增加显示亮度/对比度，并固定手画 ROI trace 来源

修改内容：

- GUI 增加 `Display` 控制：
  - `black`：显示黑点百分位
  - `white`：显示白点百分位
  - `Reset display`：恢复默认显示
- 这些设置只影响 GUI 显示，不改变原始 movie，也不改变 trace
- suite2p ROI 的 trace 继续来自 suite2p `F.npy` / `Fneu.npy`
- 手画 ROI 的 trace 改为固定使用 suite2p 主线底片：
  - 优先 `04_spatial_highpass`
  - 没有时回退到 `03_motion_correct`
- 保存 summary 中写入当前显示 movie 和手画 ROI trace movie，方便追踪来源

验证：

- `05e_roi_curation_gui.py` 语法检查通过
- offscreen 模式确认调 display black/white 会改变显示参数但不改变手画 ROI trace
- offscreen 模式确认切换显示底片后，手画 ROI trace 仍来自固定 trace movie

### 2026-06-03 03:45 05 改回 motion-corrected 输入，旧 suite2p pick 可转固定 ROI

修改内容：

- step 05 suite2p 默认输入改为 `03_motion_correct`
- `04_spatial_highpass` 仍可作为 05e 的显示底片，用来辅助看边界
- 05e 手画 ROI trace 改为优先从 motion-corrected movie 计算
- 旧保存结果重新加载时，只把 ROI 形状和位置当作有效信息
- 如果旧保存的 suite2p pick 在重跑后编号对不上或形状不匹配，会导入成 `imported_suite2p` 固定 ROI
- 旧 trace / 旧 Fneu / 旧 dF/F 不会沿用，会按当前 trace movie 和当前 neuropil 规则重新计算
- 新保存的 `manual_roi_set.json` 会为 suite2p pick 记录边界点，方便以后重跑后恢复形状

验证：

- `05_suite2p_roi_detection_schema_aligned_connected.py` 语法检查通过
- `05e_roi_curation_gui.py` 语法检查通过
- offscreen 模式确认旧 suite2p pick 形状可导入为固定 ROI

### 2026-06-03 04:05 05e 分离左侧 suite2p 参考层和右侧最终 ROI 显示

修改内容：

- `show suite2p refs` 只控制左边 suite2p 参考层
- 已选进最终 ROI set 的 suite2p ROI，在右边会继续显示
- 新增 `show final ROIs on right`
- 关闭 `show final ROIs on right` 时，右边已有 ROI 暂时隐藏，但不会丢失
- 隐藏右边 ROI 时，鼠标不会点中这些隐藏 ROI，避免误删

验证：

- `05e_roi_curation_gui.py` 语法检查通过
- offscreen 模式确认关闭 `show suite2p refs` 后，右边已选 suite2p ROI 仍可绘制
- offscreen 模式确认关闭 `show final ROIs on right` 后，右边隐藏最终 ROI set

### 2026-06-03 10:45 ROI 主线重组：05 候选库、manual 人工校对、06 重新抽 trace

修改内容：

- 当前 ROI 主线调整为：
  - 05 suite2p 只负责生成 ROI 候选库
  - manual GUI 负责人工挑选 suite2p 候选 ROI，并允许手画补 ROI
  - 06 只使用 ROI 位置和形状，默认从 motion-corrected movie 重新抽 F、Fneu 和 dF/F
- step 05 默认输入改回 `04_spatial_highpass`
  - high-pass 底片更适合 suite2p 找 ROI 边界
  - suite2p 在 high-pass movie 上算出的 F/Fneu 不作为后续主信号
- `05e_roi_curation_gui.py` 改名为 `05_manual_roi_curation_gui.py`
  - 总入口 step id 改为 `manual`
  - 输出文件夹仍保留 `05e_roi_manual_curation`，避免破坏旧保存结果
- `run_pipeline.py` 新增主流程分组：
  - `premanual` = 00-05
  - `manual` = 人工 ROI 校对 GUI
  - `postmanual` = 06-16
- 修正 step 选择逻辑
  - `premanual` 展开出来的 `05` 不再误选到 manual GUI
- 05b/05c/05d 自动 ROI 质控路线从 `current/` 归档到：
  - `archive/legacy_roi_quality_steps/`
- 06 去掉对 05c curated ROI 的依赖
  - `--roi-source auto` 现在只表示使用 suite2p `iscell.npy`
  - 默认 trace source 改为 `motion-corrected`
  - summary JSON 写入 `trace_source`、`trace_movie_path`、`neuropil_source`
- manual GUI 的 trace 显示也统一改为从 motion-corrected movie 按 ROI mask 重新计算
  - suite2p 候选 ROI 不再直接显示 suite2p 的 `F.npy/Fneu.npy`
  - 保存 suite2p-compatible `F.npy/Fneu.npy` 时也使用重新计算的 trace
- `docs/run_pipeline_command_guide.md` 重写成人话版命令说明
- `docs/roi_curation_gui.md` 和 `README.md` 更新为当前主线

清理和运行：

- 清理 test dataset 中旧的自动 ROI 输出：
  - `05_suite2p_roi_detection`
  - `05b_manual_roi_prior`
  - `05c_roi_quality_filter`
  - `05d_independent_roi_candidates`
- 没有删除 `05e_roi_manual_curation`，避免误删人工校对结果
- 重跑 05 suite2p：
  - 输入：`test_dataset/04_spatial_highpass`
  - 输出：`test_dataset/05_suite2p_roi_detection`
  - 6 个 trial 全部成功
- 重跑 06：
  - 6 个 trial 全部成功
  - summary 确认 `trace_source = motion_corrected_movie`
- 重跑 07-16：
  - 07-12、14-16 成功
  - 13 Leiden 因本机缺少 `igraph` 按预期 soft-skip
  - 15 汇总得到 total ROI = 2139
  - 16 生成 `test_dataset/16_reports/global_report.html`

验证：

- `python3 -m py_compile` 检查通过：
  - `run_pipeline.py`
  - `05_suite2p_roi_detection_schema_aligned_connected.py`
  - `05_manual_roi_curation_gui.py`
  - `06_extract_dff.py`
- `run_pipeline.py --steps premanual --dry-run` 只选择 00-05
- `run_pipeline.py --steps manual --dry-run` 只选择 manual GUI
- `run_pipeline.py --steps postmanual --dry-run` 正确选择 06-16

当前判断：

- 05 用 high-pass 做 ROI 候选检测是合理的，因为目标只是找到可能的边界。
- 后续 trace 必须从 motion-corrected movie 重新抽，避免 high-pass 处理改变真实荧光信号。
- 05b/05c/05d 这条自动筛选路线暂时不作为主线；真正限制结果质量的环节转为 manual GUI 人工校对。

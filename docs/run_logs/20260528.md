# 20260528 运行日志

## 概要

重点：继续修正 step 02 的刺激时间基准。昨天已经能用刺激记录和 `.oir` 绝对时间戳判断 trial 对应哪套刺激程序、哪些 trial 是 `nostim`。今天进一步确认：刺激日志适合决定 trial 身份和刺激参数，真正用于分析的 onset/offset 时间应依据显微镜同步记录到的 stim analog 电压通道。

## Progress

### 2026-05-28 10:57 Step 02 时间基准

关键判断：

```text
刺激时间要依据电压时间。
```

原则：

- 刺激日志 / motor rotation 记录负责：
  - trial 对应哪套刺激程序
  - 刺激模式、角度、pulse 参数等 metadata
  - `nostim` 判断
- stim analog 电压通道负责：
  - 真正刺激 onset/offset
  - 后续分析的主要时间基准

初步代码方向：

- `stim_events.csv` 的 `start_time_sec` / `end_time_sec` 优先使用 analog 电压 block
- `timing_source` 标记为 `analog_voltage_with_protocol_metadata`
- 没有可匹配 analog block 时才回退到 protocol absolute time

新问题：

- 当前 step 01 输出的 `_Stim_Analog.tif` 是 Z projection 之后的结果
- 对短 pulse 和线扫描数据，projected 版本可能已经损失更精细的电压时间结构

### 2026-05-28 11:00 Step 01 需要导出未投影 stim analog

方向：

- step 01 输出 stim analog 时不要 projection
- 或至少可选择是否 projection
- 未投影 stim analog 理论上更能保留短 pulse 时间结构

当前已有参数：

```bash
--stim-export-mode projected/raw/both
```

建议使用：

```bash
python3 /Users/dingyifei/Documents/calcium-imaging-pipeline-new/calcium-imaging-pipeline-new/current/run_pipeline.py \
  --steps 01 \
  --data-root /Users/dingyifei/Documents/calcium-imaging-pipeline-new/test_dataset \
  --action overwrite \
  --stim-export-mode both
```

`both` 输出：

```text
*_Stim_Analog.tif
*_Stim_Analog_Raw/
```

第一次运行报错：

```text
python3: can't open file '.../test_dataset/current/run_pipeline.py'
```

原因：

- 终端位于 `test_dataset/`
- 相对路径 `current/run_pipeline.py` 被解释成 `test_dataset/current/run_pipeline.py`

正确命令：

```bash
cd /Users/dingyifei/Documents/calcium-imaging-pipeline-new/calcium-imaging-pipeline-new

python3 current/run_pipeline.py \
  --steps 01 \
  --data-root /Users/dingyifei/Documents/calcium-imaging-pipeline-new/test_dataset \
  --action overwrite \
  --stim-export-mode both
```

或：

```bash
python3 /Users/dingyifei/Documents/calcium-imaging-pipeline-new/calcium-imaging-pipeline-new/current/run_pipeline.py \
  --steps 01 \
  --data-root /Users/dingyifei/Documents/calcium-imaging-pipeline-new/test_dataset \
  --action overwrite \
  --stim-export-mode both
```

### 2026-05-28 14:20 Step 01 内存和原始文件完整性

问题：

- `BF.openImagePlus()` 读取 `.oir` 时出现 `java.lang.OutOfMemoryError: Java heap space`
- 原因是 Bio-Formats 按 Full RAM 方式打开大 `.oir`，在 Mac 上容易一次性耗尽 Java heap

修正：

- step 01 改为 Bio-Formats virtual mode，打开 `.oir` 时不再一次性读入整部电影
- Z projection 继续使用逐时间点 safe 模式，优先降低峰值内存
- raw stim analog 不再保存成单个巨大 `_Stim_Analog_Raw.tif`
- raw stim analog 改为 `_Stim_Analog_Raw/` 文件夹，每个时间点保存一个小 z-stack TIFF，并写入 `manifest.json`
- step 01 开始前检查 Olympus 分卷文件是否连续，缺 `_00002` 这类分卷时跳过该 trial，并清理本步骤残留输出

测试结果：

- `test_dataset` 中 6 个 `.oir` trial 被扫描
- 5 个完整 trial 已生成或确认已有 step 01 输出
- 1 个 trial `20260428_Euprymna_retina_25x` 缺少 `_00002` 分卷，被标记为 invalid/skipped
- step 01 最终以 `skip` 模式成功完成，没有再次出现 Java heap OOM

### 2026-05-28 15:04 Step 02 raw stim analog

修正：

- step 02 新增 `--analog-source auto/raw/projected`
- 默认 `auto` 优先读取 `_Stim_Analog_Raw/manifest.json`，没有 raw 时回退到 `_Stim_Analog.tif`
- raw stim analog 读取时，每个时间点读取一个 z-stack TIFF
- 默认 `--raw-z-strategy planes`，把每个时间点里的 z 平面按顺序展开成更密的时间轴，减少 z projection / z 平均对短 pulse 的稀释
- `brightness_trace.csv` 和 `stim_map.csv` 增加 `analog_source`、`raw_z_index`、`raw_z_strategy`
- 总入口 `run_pipeline.py` 已转发 `--analog-source` 和 `--raw-z-strategy`

测试结果：

- step 02 使用 raw stim analog 重跑成功
- 5 个完整 trial 处理成功，0 failed
- `stim_map.csv` 中 5 个 trial 的 `analog_source` 均为 `raw_stim_analog_z_plane_sequence`
- `stim_events.csv` 输出 80 行事件
- `stim_pulse_events.csv` 输出 400 行 pulse 事件

### 2026-05-28 15:18 Step 02 figure PDF 输出

修正：

- step 02 的所有 matplotlib figure 同时保存 PNG 和 PDF
- PNG 用于快速预览
- PDF 用于 Illustrator / Affinity / Inkscape 等软件后期编辑
- 覆盖范围：
  - `*_stim_trace.png/pdf`
  - `*_stim_schematic.png/pdf`
  - `*_stim_pulse_trace.png/pdf`

测试结果：

- step 02 重新运行成功
- 5 个完整 trial 处理成功，0 failed
- `02_stim_map/` 中生成 11 个 PDF figure

### 2026-05-28 15:37 Step 02 图内刺激参数标注

修正：

- step 02 的 trace / schematic / pulse trace 图内增加刺激参数信息框
- 标注内容包括：
  - protocol id
  - stim mode
  - stimulus 数量
  - clock match delta
  - initial delay
  - interval
  - sustain stim_on 或 pulse count / on / off / packet duration
  - angle 列表
  - analog source / raw strategy

测试结果：

- step 02 重新运行成功
- 5 个完整 trial 处理成功，0 failed
- `02_stim_map/` 中 PNG 和 PDF figure 均为 11 个
- 抽查 pulse trace 图，参数框位于图内左上角，未遮挡主要刺激峰

### 2026-05-28 15:45 Step 03 刺激曲线不再重复复制

问题：

- `03_motion_correct/` 中出现刺激曲线图和 brightness trace
- 原因是 step 03 会从 step 02 复制 sidecar 文件，其中包括 `*_stim_trace.png` 和 `*_brightness_trace.csv`
- step 04 / 05 也有继续复制这些 sidecar 的逻辑，可能导致刺激图一路重复出现在后续步骤文件夹

修正：

- step 03 不再复制 `*_stim_trace.*`、`*_stim_schematic.*`、`*_stim_pulse_trace.*`、`*_brightness_trace.csv`
- step 03 只保留后续可能需要的刺激 CSV：
  - `*_stim_map.csv`
  - `*_stim_events.csv`
  - `*_stim_pulse_events.csv`
- step 04 / 05 同步改为只传递这些刺激 CSV，不再传播刺激图
- 清理 test dataset 中 `03_motion_correct/` 里已有的刺激曲线和 brightness trace 残留

测试结果：

- 03 / 04 / 05 脚本语法检查通过
- step 03 dry-run 命令正常
- `03_motion_correct/` 中已无 `stim_trace` / `brightness_trace` / `stim_schematic` / `stim_pulse_trace` 文件

### 2026-05-28 16:32 run_pipeline 命令说明与 Step 04

新增：

- 新增中文命令行说明文件：
  - `docs/run_pipeline_command_guide.md`
- 说明内容包括：
  - 总入口基本格式
  - `skip` / `overwrite` / `dry-run` / `step-dry-run` 的区别
  - step 00 到 step 04 的常用运行命令
  - 多步骤连续运行示例

Step 04 调整：

- `run_pipeline.py` 已能把 04 的参数转发给 `04_spatial_highpass.py`
  - `--sigma-px`
  - `--clip-negative`
  - `--dpi`
- step 04 preview 图同时保存：
  - `preview_before_after.png`
  - `preview_before_after.pdf`
- step 04 只继续传递 metadata 和刺激 CSV，不再复制 step 02 的刺激曲线图

测试结果：

- `run_pipeline.py` 和 `04_spatial_highpass.py` 语法检查通过
- step 04 launcher dry-run 正常，命令中已包含 `--sigma-px 12.0 --dpi 150`
- step 04 script dry-run 正常，找到 5 个 corrected movie
- 未实际运行空间高通滤波，未生成新的 04 输出电影

### 2026-05-28 17:07 Step 05 suite2p 入口参数

修正：

- step 05 ROI overlay 同时保存 PNG 和 PDF
- `run_pipeline.py` 已能把常用 suite2p 参数转发给 step 05：
  - `--cell-diameter-um`
  - `--diameter-scale`
  - `--threshold-scaling`
  - `--suite2p-threads`
  - `--n-workers`
  - `--overlay-dpi`
  - 以及其他 suite2p 检测 / neuropil 参数
- 中文命令行说明中新增 step 05 运行示例和参数解释

测试结果：

- `run_pipeline.py` 和 step 05 语法检查通过
- launcher dry-run 正常，命令中已包含 step 05 参数
- step 05 script dry-run 正常，读取 `04_spatial_highpass/`
- 当前 `skip` 模式下 5 个 trial 均被跳过，说明已有 suite2p 必需输出；如需用新参数重跑，需要使用 `--action overwrite`

### 2026-05-28 18:08 Step 06 dF/F 初版

新增：

- 新增 `current/06_extract_dff.py`
- 结构沿用当前主线脚本风格：
  - `STEP_NAME`
  - `STEP_OUTPUT_PATTERNS`
  - `TrialInput`
  - `RunSummary`
  - `skip / overwrite / dry-run`
  - 每个 trial 单独输出 summary JSON
  - 每步结束打印 found / processed / skipped / failed
- 默认输入：
  - `DATA_ROOT/05_suite2p_roi_detection/`
- 默认输出：
  - `DATA_ROOT/06_dff/<trial>/`

核心逻辑：

- 读取 suite2p 输出：
  - `F.npy`
  - `Fneu.npy`
  - `iscell.npy`
  - `stat.npy`
  - `ops.npy`
- neuropil 使用 suite2p 的 `Fneu.npy`
- 默认 neuropil correction：
  - `F_corrected = F - 0.7 * Fneu`
- 默认只保留 `iscell[:, 0] == 1`
- 可用 `--include-noncell` 保留全部 ROI
- 默认 F0：
  - `--f0-mode percentile`
  - `--f0-percentile 10`
- 预留：
  - `rolling-percentile`
  - `rolling-median`
  - `stim-baseline`
- dF/F 分母使用 `--f0-eps` 防止 F0 太小导致数值爆炸

输出设计：

- `<trial>_roi_table.csv`
- `<trial>_F_raw.npy`
- `<trial>_Fneu.npy`
- `<trial>_F_corrected.npy`
- `<trial>_F0.npy`
- `<trial>_dff.npy`
- `<trial>_dff_traces.csv`
- `<trial>_dff_summary.csv`
- `<trial>_dff_heatmap.png/pdf`
- `<trial>_example_traces.png/pdf`
- `<trial>_dff_summary.json`

测试结果：

- `python3 -m py_compile current/06_extract_dff.py` 通过
- 默认 `python3` 环境缺少 matplotlib，直接运行 06 会停在导入画图库
- `caiman` 环境 dry-run 通过
- dry-run 找到 5 个 suite2p trial
- 未正式读取 suite2p 大数组，未生成 `06_dff` 输出

版本管理规则：

- 后续代码迭代保持 git diff 可追踪
- 每轮功能性修改后更新运行日志
- 不自动 commit；需要提交时再单独整理提交

### 2026-05-28 19:05 Step 06 按功能重写并接入总入口

修正：

- `current/06_extract_dff.py` 按 06 的功能要求重新组织
- 重点围绕 suite2p 输出到 dF/F 的单一步骤职责：
  - 读取 `F.npy`
  - 读取 suite2p 的 `Fneu.npy`
  - 读取 `iscell.npy`
  - 读取 `stat.npy`
  - 读取 `ops.npy`
- 明确 neuropil 来源为：
  - `suite2p_Fneu.npy`
- 不使用手画 neuropil
- matplotlib 改为只在真正画图时导入，dry-run 更轻
- 保留输出：
  - ROI table
  - raw / neuropil / corrected / F0 / dF/F NPY
  - trace CSV
  - summary CSV / JSON
  - heatmap PNG/PDF
  - example traces PNG/PDF

总入口：

- `run_pipeline.py` 已加入 step 06
- step 06 使用 `caiman` 环境
- 入口常用参数：
  - `--neuropil-coeff`
  - `--f0-mode`
  - `--f0-percentile`

测试结果：

- `current/06_extract_dff.py` 和 `run_pipeline.py` 语法检查通过
- launcher dry-run 正常，命令中包含 step 06 参数
- step 06 script dry-run 正常
- `--action overwrite --step-dry-run` 显示 5 个 trial would process
- 当前 `test_dataset/06_dff/` 已存在完整 06 输出；这些输出未删除、未覆盖

注意：

- 系统默认 `python3` 环境缺少 numpy，不适合直接运行分析步骤
- 总入口会用 conda `caiman` 环境运行 step 06
- 后续 07 及以后可以先写第一版主线脚本，再逐步逐步精修

### 2026-05-28 19:56 Step 06 清理旧输出并正式重跑

处理：

- 清理 `test_dataset/06_dff/` 旧输出
- 修正 dF/F 分母稳定逻辑：
  - 使用 `max(abs(F0), eps)` 作为分母
  - 避免负 F0 被替换成 `1e-6` 后造成 dF/F 爆炸
- 使用总入口正式重跑 step 06

命令参数：

```text
--neuropil-coeff 0.7
--f0-mode percentile
--f0-percentile 10
```

测试结果：

- step 06 处理 5 个 trial
- 0 failed
- 输出重新生成到 `test_dataset/06_dff/`
- summary 中 dF/F 不再出现几千万级均值

注意：

- 部分 trial 仍存在较大 dF/F 峰值，后续可能需要继续比较 `stim-baseline`、`rolling-percentile` 等 F0 策略

### 2026-05-28 20:00 Step 07 calcium event detection 初版

新增：

- 新增 `current/07_detect_events.py`
- 默认输入：
  - `DATA_ROOT/06_dff/`
- 默认输出：
  - `DATA_ROOT/07_events/<trial>/`
- 术语使用 calcium event，不默认称为 spike

实现：

- 方法 A：
  - `robust-threshold`
  - median + MAD
  - `robust_sigma = 1.4826 * MAD`
  - 默认 `--event-threshold-sigma 3.0`
- 方法 B：
  - `find-peaks`
  - 使用 scipy `find_peaks`
  - 支持 prominence / height / distance / width 参数

输出：

- `<trial>_event_table.csv`
- `<trial>_event_binary.npy`
- `<trial>_event_mask.npy`
- `<trial>_event_rate_by_roi.csv`
- `<trial>_event_summary.csv`
- `<trial>_event_summary.json`
- `<trial>_event_raster.png/pdf`
- `<trial>_event_examples.png/pdf`

总入口：

- `run_pipeline.py` 已加入 step 07
- step 07 使用 `caiman` 环境
- 入口常用参数：
  - `--event-method`
  - `--event-threshold-sigma`

测试结果：

- step 07 和总入口语法检查通过
- step 07 dry-run 通过
- dry-run 找到 5 个 dF/F trial
- 未正式生成 `07_events` 输出

## Issues / Notes

最终逻辑：

```text
trial identity / stim parameters: stimulus logs + absolute timestamps
precise onset/offset: stim analog voltage channel
```

- step 02 已优先读取 `_Stim_Analog_Raw/`
- raw trace 当前使用 z-plane 展开后的密集时间轴，pulse trial 已能在电压图中显示 packet 内多个 flash
- trace / schematic 图已带刺激参数标注，PDF 中可继续编辑
- step 03 及后续步骤不再重复保存刺激曲线图；刺激图集中保留在 `02_stim_map/`
- 命令行说明已整理到 `docs/run_pipeline_command_guide.md`
- step 04 已接入总入口，可先用 dry-run 检查，再手动正式运行
- step 05 已接入总入口常用参数，ROI overlay 支持 PDF 输出
- step 06 已按功能重写并接入总入口，使用 suite2p 的 `Fneu.npy` 做 neuropil correction
- step 07 初版已完成并接入总入口，dry-run 通过
- 仍需要继续人工检查图是否符合真实刺激时间
- `20260428_Euprymna_retina_25x` 原始 OIR 分卷不完整，缺 `_00002`，该 trial 暂不应进入后续分析

## Next

- 人工检查 02 输出图：
  - `*_stim_trace.png`
  - `*_stim_trace.pdf`
  - `*_stim_schematic.png`
  - `*_stim_schematic.pdf`
  - pulse trial 的 `*_stim_pulse_trace.png`
  - pulse trial 的 `*_stim_pulse_trace.pdf`
- 比较 raw trace 与 projected trace 的 pulse 可见性
- 如 raw 单 z 平面仍不理想，继续尝试 ROI/局部区域提取刺激通道
- 手动运行 step 04，检查 `04_spatial_highpass/` 中的 preview PNG/PDF 和输出 movie
- step 04 结果确认后，用 `--action overwrite` 重跑 step 05，比较 ROI 数量和 overlay 质量
- 正式运行 step 07，检查 event raster 和 event examples
- 下一步开发 08 stimulus response analysis

## 今日结论

刺激时间基准已经明确：刺激日志负责匹配 trial 和读取参数，真正的分析时间应以显微镜记录的 stim analog 电压通道为准。step 01 已能在 Mac 上用低内存方式导出 projected stim analog 和未投影 raw stim analog 文件夹；step 02 已优先读取 raw stim analog，并成功重新生成测试数据输出。step 03 以后不再重复保存刺激曲线图，step 04、step 05、step 06、step 07 已接入总入口；step 06 已正式重跑，step 07 已通过 dry-run。

# 2026-07-06 Run Log

## 概要

- 修复 Linux workstation 上 `step 01` 的 Fiji worker 日志编码风险。
- 将 `current/preprocess/01_fiji_totif_worker.py` 的运行时输出统一改为英文 ASCII 文本，避免 Jython/Fiji 执行路径中再次混入中文日志。

## Progress

- 修改文件：
  - `current/preprocess/01_fiji_totif_worker.py`
- 处理内容：
  - 将 warning / skip / overwrite / metadata / batch summary / main loop error 等运行期日志全部改为英文。
  - 去除该文件中的全部非 ASCII 字符，确保 worker 源文件本身也不再携带中文文本。

## Issues / Notes

- 本次直接触发的报错发生在 Linux Fiji headless 运行 `01_fiji_totif_worker.py` 时，日志显示：
  - `Main loop error: 'ascii' codec can't decode byte ...`
- 现象与 worker 侧混用中文输出高度一致，因此先做最小可靠修复：清掉 worker 里的非 ASCII 日志文本。
- 这次没有扩展到整个 pipeline 的所有步骤；当前只覆盖 `step 01` 的 Fiji/Jython 执行路径。

## 验证

- 已检查 `current/preprocess/01_fiji_totif_worker.py` 中非 ASCII 字符数量，结果为 `0`。
- 未在 Linux workstation 上重跑真实 `step 01`。
- 原因：
  - 真实验证依赖 Fiji/Jython 运行环境与实际 OIR 数据；
  - 当前仅完成本地静态修复与文件内容检查，不能假装代表运行已通过。

## Next

- 在 workstation 上重跑原命令或至少重跑 `step 01`，确认不再出现 `ascii codec` 报错。
- 若仍失败，继续抓取新的第一现场报错，优先判断是否还有：
  - Fiji/Jython 对 stdout/stderr 的编码问题；
  - OIR 内部 metadata 解码问题；
  - 外部插件/JAR 输出混入非 UTF-8 文本。

## 追加处理：统一 pipeline 运行日志为英文

- 按“只改实际运行日志，不改文档和实验内容识别逻辑”的边界，继续统一了当前主线入口的日志文本：
  - `current/run_pipeline.py`
  - `current/preprocess/00_oir_file_manager.py`
  - `current/preprocess/01_fiji_totif_ini.py`
  - `current/preprocess/02_generate_stim_map.py`
  - `current/preprocess/03_motion_correct_func_caiman.py`
  - `current/preprocess/04_spatial_highpass.py`
- 处理内容：
  - step 名称改为英文；
  - launcher / premanual 终端日志改为英文；
  - `skip / dry-run / summary / error / warning` 等状态文本统一为英文。
- 保留未改部分：
  - README、run log、注释；
  - `trial_context_utils.py` 中用于识别中文实验记录内容的逻辑，因为那不是日志，改动会影响功能。

## 追加验证

- 已检查 `current/` 下所有 `LOGGER(...)`、`print(...)`、`banner(...)` 所在行：
  - 不再存在中文运行日志文本。
- 已运行 `python3 -m py_compile`：
  - `current/run_pipeline.py`
  - `current/preprocess/00_oir_file_manager.py`
  - `current/preprocess/01_fiji_totif_ini.py`
  - `current/preprocess/02_generate_stim_map.py`
  - `current/preprocess/03_motion_correct_func_caiman.py`
  - `current/preprocess/04_spatial_highpass.py`
- `current/preprocess/01_fiji_totif_worker.py` 未使用 `py_compile` 作为有效验证：
  - 该脚本是 Fiji/Jython worker；
  - 其中 `from loci.plugins.in import ImporterOptions` 对 CPython 语法检查天然不兼容；
  - 本次未对其运行逻辑做进一步改动，仅沿用前面已完成的英文 ASCII 日志修复。

## 追加处理：手工 stim log 生成与 step-02 兼容

- 新增脚本：
  - `current/tools/generate_manual_stim_logs_from_notes.py`
- 目的：
  - 从 Evernote 导出的钙成像实验记录中，提取“只有手工记录、没有 controller export”的刺激信息；
  - 生成兼容 step 02 的 `timestamp_log_*.csv`、`stim_map_*.csv`、`experiment_config_*.json`。
- 同时修改：
  - `current/preprocess/02_generate_stim_map.py`
- 兼容逻辑变化：
  - 如果 stimulus protocol 的 `run_id` 或 `target_trial_id` 与当前 `trial_id` 完全一致，step 02 现在可以直接选用这份 protocol；
  - 即使没有 `host_timestamp`，也可以退回使用 protocol 里的相对 `mcu_onset_sec / mcu_offset_sec` 生成事件时间；
  - 因而手工生成的 stim log 不再强依赖真实绝对时钟。

## 手工 stim log 本地结果

- 已把可稳定解析的手工刺激记录写入：
  - `/Users/dingyifei/Documents/calcium-imaging-pipeline-new/outputs/manual_stim_logs`
- 当前自动成功生成的 trial 共 16 个：
  - `20251014`：`0003`, `0005`, `0007`
  - `20251205`：`0002`, `0004`, `0006`, `0008`, `0010`, `0012`
  - `20260212`：`0001`, `0002`, `0004`, `0005`, `0007`, `0008`, `0013`
- 跳过的其余日期/trial 主要原因不是脚本报错，而是实验记录信息不足：
  - 只有 `nostim`
  - 只有笼统的 `50-100 stim`
  - 缺少事件数、角度列表、初始延迟或间隔
  - 明显异常设置，如 `每60帧` 这类需人工确认的 trial

## 同步状态

- 已准备好要同步的两类内容：
  - 主线代码修复
  - `outputs/manual_stim_logs/.../manual_notes/` 生成的手工 stim log
- 已尝试对 workstation 做 `ssh` / `rsync --dry-run` 预览。
- 当前未完成实际同步，原因是 workstation SSH 连接被对端中途关闭：
  - `Connection closed by 100.68.63.50 port 22`
- 因此这次只能确认：
  - 本地代码修复完成；
  - 本地手工 stim log staging 完成；
  - 远端同步尚未成功，不能假装已完成。

## Future

- 如果后续决定把整套 pipeline 终端输出统一成英文，可继续覆盖：
  - `current/preprocess/01_fiji_totif_ini.py`
  - `current/run_pipeline.py`
  - 其余 preprocess / analysis 步骤

## 今日结论

- 当前问题首先不是 stim log 缺失，也不是 conda 环境缺失，而是 `step 01` 的 Fiji worker 编码风险。
- 已完成最小范围修复：`01_fiji_totif_worker.py` 运行日志改为英文 ASCII，并清除文件内全部非 ASCII 文本。

## 追加处理：workstation-campus 联调与 20251205 实机验证

- 连接方式：
  - 已改用 `ssh workstation-campus` 对工作站联调；
  - `ssh workstation` 这一条 tailscale 路径本次未继续作为执行通道。
- 已同步到工作站的代码：
  - `current/run_pipeline.py`
  - `current/preprocess/00_oir_file_manager.py`
  - `current/preprocess/01_fiji_totif_ini.py`
  - `current/preprocess/01_fiji_totif_worker.py`
  - `current/preprocess/01_fiji_totif_worker_wrapper.py`
  - `current/preprocess/02_generate_stim_map.py`
  - `current/preprocess/03_motion_correct_func_caiman.py`
  - `current/preprocess/04_spatial_highpass.py`
  - `current/tools/generate_manual_stim_logs_from_notes.py`

## 追加修复：手工 stim log 与真实 trial id 对齐

- 新问题：
  - 实验记录中的 trial 名不总是等于原始数据目录名；
  - 例如 `20251205` 记录写的是 `20251205_Euprymna_0002`，但工作站原始数据实际是 `20251205_Euprymna_retina_0002`。
- 已修复：
  - `current/tools/generate_manual_stim_logs_from_notes.py` 现在支持通过 `metadata.csv` 把实验记录里的 trial 名解析到真实 `Trial_ID`；
  - 生成的 `experiment_config_*.json` 会保留：
    - `run_id`
    - `target_trial_id`
    - `source_note_trial_id`
- 本次重新生成并同步后，`20251205` 的手工 bundle 已包含正确 trial id 版本：
  - `20251205_Euprymna_retina_0002`
  - `20251205_Euprymna_retina_0004`
  - `20251205_Euprymna_retina_0006`
  - `20251205_Euprymna_retina_0008`
  - `20251205_Euprymna_retina_0010`
  - `20251205_Euprymna_retina_0012`

## 追加修复：step 01 单 trial 执行链

- 新发现的直接阻断问题：
  - Linux 上 `/home/yifei/Fiji/fiji` 这一类 launcher 需要 `--run` 才会真正执行 worker；
  - 旧逻辑会出现 Fiji 退出码为 0、但 worker 实际没跑的“假成功”。
- 已修复：
  - `current/preprocess/01_fiji_totif_ini.py`
  - `current/preprocess/01_fiji_totif_worker_wrapper.py`
  - `current/preprocess/01_fiji_totif_worker.py`
- 具体修复点：
  - launcher 对 `fiji` 入口改为显式 `--run`；
  - ASCII wrapper 现在同时兼容 Fiji 脚本参数注入与旧的 CLI 参数解析；
  - 当 `--data-root` 直接指向单个 trial 文件夹时，step 01 输出路径不再错误落到 `01_oir_to_tif/..` 外面。

## 追加验证：20251205_Euprymna_retina_0002

- 工作站 step 01：
  - 已用单 trial 路径成功命中 `20251205_Euprymna_retina_0002`；
  - 输出存在于：
    - `/mnt/50d357b2-473b-4533-8c74-1db99704876a/yifeiding/calcium_imaging/2025_olympus/01_oir_to_tif/20251205_Euprymna_retina_0002`
  - 已确认存在：
    - `20251205_Euprymna_retina_0002_Max_Proj.tif`
    - `20251205_Euprymna_retina_0002_metadata.json`
- 工作站 step 02：
  - 额外修复了 `process_trial()` 没把 `trial_id` 传给 `match_stim_protocol()` 的问题；
  - 重新运行时使用 `python -B`，避免旧 `.pyc` 缓存干扰源码验证。
- 当前结果：
  - `20251205_Euprymna_retina_0002` 已成功匹配到手工 protocol：
    - `stim_protocol_id = 20251205_Euprymna_retina_0002`
    - `number_of_stim = 20`
    - `initial_delay_sec = 60.0`
    - `avg_duration_sec = 1.2`
    - `avg_interval_sec = 60.0`
    - `timing_source = protocol_relative_time_fallback`
    - `analog_source = no_analog_input`
  - step 02 输出存在于：
    - `/mnt/50d357b2-473b-4533-8c74-1db99704876a/yifeiding/calcium_imaging/2025_olympus/02_stim_map/20251205_Euprymna_retina_0002`

## 现阶段边界

- 这次已确认：
  - 工作站代码同步完成；
  - 手工 stim log 同步完成；
  - `20251205_Euprymna_retina_0002` 可在工作站上走通 step 01 与 step 02；
  - step 02 会用手工 stim log 生成兼容输出。
- 这次未做：
  - 未删除早先错误命名的手工 bundle；
  - 未批量重跑 `20251205` 其余 stimulated trial；
  - 未对其余 205 个信息不足的实验记录做人为猜测式补全。

## 追加验证：20251205 全部 stimulated trial 已录入

- 已在工作站上批量处理 `20251205` 这 6 个有刺激的 trial：
  - `20251205_Euprymna_retina_0002`
  - `20251205_Euprymna_retina_0004`
  - `20251205_Euprymna_retina_0006`
  - `20251205_Euprymna_retina_0008`
  - `20251205_Euprymna_retina_0010`
  - `20251205_Euprymna_retina_0012`
- 处理方式：
  - 仅使用已同步的手工 stim log；
  - step 01 输出已确认存在；
  - step 02 通过 `python -B` 重跑，避免旧 `.pyc` 缓存影响源码验证。
- 当前结果：
  - 6 个 trial 均成功匹配到各自手工 protocol；
  - 每个 trial 的 `stim_type = sustain`；
  - 每个 trial 的 `number_of_stim = 20`；
  - `initial_delay_sec = 60.0`；
  - `avg_duration_sec = 1.2`；
  - `avg_interval_sec = 60.0`；
  - 每个 `stim_events.csv` 均为 20 条事件。
- 工作站输出位置：
  - `/mnt/50d357b2-473b-4533-8c74-1db99704876a/yifeiding/calcium_imaging/2025_olympus/02_stim_map/<trial_id>/`

## 追加验证：20251205 明确 nostim 的 trial 已按最新 no-analog 方式处理

- 已处理的明确 `nostim` trial：
  - `20251205_Euprymna_retina`
  - `20251205_Euprymna_retina_0003`
  - `20251205_Euprymna_retina_0005`
  - `20251205_Euprymna_retina_0007`
  - `20251205_Euprymna_retina_0009`
  - `20251205_Euprymna_retina_0011`
- 处理方式：
  - 保持无手工 protocol；
  - 直接使用最新版 step 02 的 `no_analog_input` 兼容路径生成标准输出。
- 当前结果：
  - 6 个 trial 均写出 step 02 输出目录；
  - `stim_type = nostim`
  - `number_of_stim = 0`
  - `stim_protocol_id` 为空
  - `analog_source = no_analog_input`
- 说明：
  - `20251205_Euprymna_retina_0001` 的实验记录写的是 `pass`，这次没有擅自归入 `nostim`。

## 追加功能：pipeline 支持按日期筛选处理

- 修改目的：
  - 解决当前 pipeline 和 dashboard GUI 只能一次性扫完整个数据根目录、无法只跑某一天数据的问题。
- 本次代码改动：
  - `current/run_pipeline.py`
    - 新增 `--date-id YYYYMMDD` 参数；
    - 当选择 `01` 步时，若存在 `/00_original_files/<date_id>`，会自动把输入范围缩到该日期目录；
    - 当选择 `02` 到 `12` 步时，会自动收集该日期下的 trial id，并以逗号列表形式下发给各步脚本；
    - 对当前尚不支持按日期筛选的步骤保留提示，避免静默全量运行。
  - `current/pipeline_dashboard_gui.py`
    - 新增 `Date id` 输入框；
    - 命令预览会同步显示 `--date-id`。
  - `current/preprocess/03_motion_correct_func_caiman.py`
  - `current/preprocess/04_spatial_highpass.py`
  - `current/roi/05_suite2p_roi_detection_schema_aligned_connected.py`
    - 以上三个脚本新增 `--trial-id`，支持接收单个 trial 或逗号分隔 trial 列表。
- 验证方式：
  - 本地执行 `py_compile`，确认新增参数和入口无语法错误；
  - 已同步到 Linux 工作站；
  - 在工作站对 `/mnt/50d357b2-473b-4533-8c74-1db99704876a/yifeiding/calcium_imaging/2025_olympus` 做 dry-run 验证：
    - `--steps 01,02,03,04,05 --date-id 20251205`
    - `--steps 06,07,08,09,10,11,12 --date-id 20251205`
- 验证结果：
  - step 01 已正确缩到：
    - `/mnt/50d357b2-473b-4533-8c74-1db99704876a/yifeiding/calcium_imaging/2025_olympus/00_original_files/20251205`
  - step 02 到 step 12 已正确只携带 `20251205_*` 这批 trial id；
  - 不再把整棵 `2025_olympus` 下的其他日期一起带入处理。
- 当前边界：
  - `manual` 仍然按单个 trial 使用，没有自动把 `date-id` 展开成 GUI 批处理；
  - `13` 之后的汇总类步骤这次没有扩展成日期级筛选。

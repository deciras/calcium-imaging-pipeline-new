# 20260527 运行日志

## 概要

重点：step 02 的 pulse 刺激时间、测试数据目录结构、刺激示意图、no-stim 判断、角度颜色映射。

核心问题：pulse 模式下实际有多个短 flash，但 projected `_Stim_Analog.tif` 可能把多个 flash 压成一个波包。需要同时保留真实电压图和刺激示意图，并让数据结构更清楚。

## Progress

### 2026-05-27 10:26 Step 02 pulse 事件拆分

- `stim_events.csv` 继续表示 analog 通道中能看出来的刺激 packet/block
- pulse 模式新增：

```text
*_stim_pulse_events.csv
02_stim_map/stim_pulse_events.csv
```

- 每个 packet 内的 flash 展开成单独行
- 初始策略：analog packet 起点 + Arduino `pulse_count` / `pulse_on_ms` / `pulse_off_ms`

运行：

```bash
python3 current/run_pipeline.py \
  --steps 02 \
  --data-root /Users/dingyifei/Documents/calcium-imaging-pipeline-new/test_dataset \
  --stim-log-root /Users/dingyifei/Desktop/20260428_motor_rotation \
  --action overwrite
```

结果：

- 找到 6 个 trial
- 找到 12 份刺激程序日志
- 6 个 trial 处理成功
- 失败 0 个
- 两个 pulse trial 各生成 200 行 pulse event
- 每个 pulse trial：20 个 packet x 10 个 flash

### 2026-05-27 10:46 数据目录扁平化

目标结构：

```text
test_dataset/
  00_original_files/
  01_oir_to_tif/
  02_stim_map/
  03_motion_correct/
  04_spatial_highpass/
  05_suite2p_roi_detection/
```

改动：

- `current/00_oir_file_manager.py` 默认使用 `00_original_files/`
- `01-05` 默认输出根目录改为 `DATA_ROOT`
- step 01 默认从 `DATA_ROOT/00_original_files/` 读取 `.oir`
- step 02 默认从 `DATA_ROOT/01_oir_to_tif/` 读取
- step 03 默认从 `DATA_ROOT/01_oir_to_tif/` 和 `DATA_ROOT/02_stim_map/` 读取
- step 04 默认从 `DATA_ROOT/03_motion_correct/` 读取
- step 05 默认优先从 `DATA_ROOT/04_spatial_highpass/` 读取，否则回退 `DATA_ROOT/03_motion_correct/`

实际迁移：

- 6 个原始 trial 文件夹移动到 `00_original_files/`
- `metadata.csv` 移动到 `00_original_files/metadata.csv`
- 原 `pipeline_outputs/01_oir_to_tif`、`02_stim_map`、`03_motion_correct` 移动到数据集根目录
- 空的 `pipeline_outputs/` 删除
- 重新生成 `test_dataset/tree.txt`

修复：

- step 01 Fiji worker 不再误跳过 `00_original_files/`
- 只跳过真正的处理步骤目录

metadata refresh：

```bash
python3 current/run_pipeline.py \
  --steps 01 \
  --data-root /Users/dingyifei/Documents/calcium-imaging-pipeline-new/test_dataset \
  --action skip \
  --metadata-mode refresh
```

结果：

- 找到 6 个 `.oir`
- 6 个 trial 全部跳过 TIFF 生成
- `Done: 0`
- 6 个 metadata JSON 刷新成功
- `source_oir_path` 指向 `00_original_files/.../*.oir`

验证：

- `00-05` dry-run 路径适配新结构

### 2026-05-27 11:16 Step 02 pulse 示意图

保留真实电压图：

```text
*_stim_trace.png
```

新增 pulse 示意图：

```text
*_stim_pulse_trace.png
```

图像含义：

- 灰色低条：analog packet
- 彩色短条：packet 内每个 flash
- stimulus index 标在图上
- 方便和 `stim_pulse_events.csv` 对照

验证：

- 6 个 trial 处理成功
- 失败 0 个
- 两个 pulse trial 生成 `*_stim_pulse_trace.png`
- skip 判断改进：已有 pulse CSV 但缺图时不再误判为完整

### 2026-05-27 11:35 Step 01 预留 raw stim channel

原因：

- projected `_Stim_Analog.tif` 可能丢失短 pulse 细节
- 更高精度需要未投影刺激通道

新增参数：

```bash
--stim-export-mode projected/raw/both
```

行为：

- `projected`：默认，不额外生成大文件
- `raw` 或 `both`：额外保存

```text
*_Stim_Analog_Raw.tif
```

验证：

- 做了 dry-run 和参数传递验证
- 未实际生成 raw TIFF

### 2026-05-27 12:10 真实电压图和统一刺激示意图

目标：

- pulse trial 和 sustain trial 都有真实电压图
- pulse trial 和 sustain trial 都有刺激示意图

新增统一示意图：

```text
*_stim_schematic.png
```

当时先尝试长记录分段多行显示；晚上改回单行完整时间轴。

### 2026-05-27 16:10 用刺激日志判断 no-stim

完整刺激记录位置：

```text
/Users/dingyifei/Documents/calcium-imaging-pipeline-new/test_dataset/test_dataset_motor_rotation
```

step 02 默认自动寻找：

```text
DATA_ROOT/test_dataset_motor_rotation/
```

当时原则：

- trial 和刺激程序配对优先用 `.oir` 绝对时间戳 + 刺激日志 host timestamp
- 匹配到刺激日志才写入 `stim_events.csv`
- 匹配不到的 trial 标记为 `nostim`
- 电压阈值检测只作为 analog 质量检查和辅助信息

清理并重跑：

```bash
rm -rf /Users/dingyifei/Documents/calcium-imaging-pipeline-new/test_dataset/02_stim_map
python3 current/run_pipeline.py \
  --steps 02 \
  --data-root /Users/dingyifei/Documents/calcium-imaging-pipeline-new/test_dataset \
  --action overwrite
```

结果：

- 6 个 trial 处理成功
- 失败 0 个
- 2 个 trial 标记为 `nostim`
  - `20260428_Euprymna_retina_25x`
  - `20260428_Euprymna_retina2_25x`
- 4 个 trial 匹配到刺激记录
  - `20260428_Euprymna_retina_25x_0001` -> `20260428_170422` sustain
  - `20260428_Euprymna_retina_25x_0002` -> `20260428_171841` pulse
  - `20260428_Euprymna_retina2_25x_0001` -> `20260428_190404` sustain
  - `20260428_Euprymna_retina2_25x_0002` -> `20260428_191809` pulse
- `stim_events.csv` 80 行
- `stim_pulse_events.csv` 400 行

后续修正：

- 2026-05-28 确认真正分析用 onset/offset 仍应来自电压通道
- 刺激日志用于 trial 身份和参数

### 2026-05-27 19:45 单行时间轴和半圆角度色盘

图像要求：

- 示意图不回行
- 电压图不回行
- 横坐标为完整时间
- 角度颜色来自固定半圆色盘，不随机

改动：

- `*_stim_trace.png`：单行完整时间轴
- `*_stim_schematic.png`：单行完整时间轴
- `*_stim_pulse_trace.png`：单行完整时间轴
- 图片根据记录时长自动变宽
- 角度颜色按 0-180 度半圆色盘固定映射
- 同一角度在所有 trial 颜色一致

运行：

```bash
python3 current/run_pipeline.py \
  --steps 02 \
  --data-root /Users/dingyifei/Documents/calcium-imaging-pipeline-new/test_dataset \
  --action overwrite
```

结果：

- 6 个 trial 成功
- 失败 0 个
- 真实电压图为宽而矮的一行图，例如 `5490 x 614`
- 刺激示意图为宽而矮的一行图，例如 `5129 x 509`
- 默认 `skip` 再跑时 6 个 trial 全部跳过

后续微调：

- 半圆色盘移到坐标轴外侧右上方
- 普通角度 legend 放到左上方外侧
- 避免遮挡曲线或刺激条

## Issues / Notes

- projected `_Stim_Analog.tif` 对短 pulse 可能不够可靠
- Z projection 和整帧平均可能抹掉 pulse 细节
- `stim_pulse_events.csv` 仍依赖 Arduino 配置展开 packet 内 flash
- 更精确方案需要未投影 `_Stim_Analog_Raw.tif`
- 2026-05-28 继续修正：刺激日志负责身份和参数，真正 onset/offset 应来自电压通道

## Next

- step 01 生成 `_Stim_Analog_Raw.tif`
- step 02 优先读取 raw stim analog
- 明确区分：
  - trial 身份和刺激参数来自刺激日志
  - 分析时间来自 stim analog 电压通道

## 今日结论

测试数据结构整理成 `00_original_files/、01_oir_to_tif/、02_stim_map/…` 扁平结构。step 02 完成 pulse 事件拆分、刺激示意图、no-stim 判断和角度配色改造。到今天结束，step 02 可以用刺激日志和 `.oir` 绝对时间戳判断 trial 身份，并输出单行时间轴的真实电压图和刺激示意图；更精确的刺激时间仍需要未投影 stim analog。

# 20260610 运行日志

## 概要

重点：归档当前代码状态，重排 `current/` 目录，并把 ROI 后的基础分析步骤整理成可由 GUI 调用的最小分析链。

## Progress

### 2026-06-10 current 代码归档与目录重排

修改内容：

- 将原 `current/` 扁平脚本归档到 `archive/20260603_code/`
- 重新整理 `current/` 结构：
  - `preprocess/`：00-04，包含文件整理、Fiji 转换、刺激表、运动矫正、空间高通
  - `roi/`：05 suite2p 候选 ROI 生成，以及 manual ROI curation GUI
  - `analysis/`：06-16，包含 dF/F、事件、刺激响应、调谐、群体分析、聚类和报告
  - `tools/`：维护工具
- 保留 `current/run_pipeline.py` 作为统一入口
- 新增 `current/README.md` 记录当前目录含义和常用步骤组
- 更新 `run_pipeline.py` 中所有 step script 路径，适配新目录结构

验证：

- `run_pipeline.py --list-steps` 可以列出新路径下的所有步骤
- `premanual`、`manual`、`postmanual` dry-run 均能正确解析对应步骤
- 常规 Python 脚本语法检查通过
- `01_fiji_totif_worker.py` 是 Fiji/Jython 脚本，不按普通 Python 语法检查

### 2026-06-10 ROI 后基础 analysis 链路

目标：

- 先把画完 ROI 后最基础、最适合接进 GUI 的分析步骤固定下来
- 暂定基础链路为：
  - 06：从 final ROI set 重新抽 trace、Fneu、F - 0.7Fneu、dF/F
  - 07：检测 calcium events
  - 08：按刺激窗口统计响应
  - 09：按刺激角度计算 tuning / preferred angle

修改内容：

- `run_pipeline.py` 新增步骤组：
  - `basic-analysis` = 06-09
  - `basicanalysis`、`basic` 作为别名
- `run_pipeline.py` 新增 per-step `accepts_trial_id` 标记
- `manual` 和 06-09 支持从统一入口接收 `--trial-id`
- 06-09 各脚本新增 `--trial-id`
- 06-09 接收到 trial ID 后只处理指定 trial
- `current/README.md` 补充常用分组说明：
  - `premanual`
  - `manual`
  - `basic-analysis`
  - `postmanual`

验证：

- 语法检查通过：
  - `current/run_pipeline.py`
  - `current/analysis/06_extract_dff.py`
  - `current/analysis/07_detect_events.py`
  - `current/analysis/08_stim_response_analysis.py`
  - `current/analysis/09_angle_tuning_analysis.py`
- dry-run 验证通过：
  - `python3 current/run_pipeline.py --steps basic-analysis --data-root ../test_dataset --trial-id 20260428_Euprymna_retina2_25x --dry-run`
  - 只选中 06-09
  - 四个步骤均正确带上同一个 `--trial-id`
- test dataset 实际运行通过：
  - `python3 current/run_pipeline.py --steps basic-analysis --data-root ../test_dataset --trial-id 20260428_Euprymna_retina2_25x`
  - 06：识别 manual curation 结果，输出 `n_roi=8`、`n_frames=500`
  - 07：输出 `n_events=55`
  - 08：顺利运行，当前 trial 的刺激响应表为 0 行
  - 09：顺利运行；由于 08 没有响应行，angle tuning ROI 数为 0

## Issues / Notes

- 08/09 在该 test trial 上没有得到有效响应行，原因更像是该 trial 的刺激响应窗口或刺激表内容没有匹配出响应，而不是流程中断。
- 运行真实 `basic-analysis` 时更新了 `test_dataset` 下 06-09 的输出。
- 工作区仍包含 current 目录重排造成的文件移动：旧扁平脚本显示为删除，新子目录文件显示为新增。

## Next

- 在 GUI 中增加一个基础分析入口，优先调用：
  - 当前 data root
  - 当前 trial ID
  - `--steps basic-analysis`
- GUI 里显示基础分析状态：
  - 是否已有 06 dF/F
  - 是否已有 07 events
  - 是否已有 08 stim response
  - 是否已有 09 angle tuning
- 对 08 刺激响应为 0 行的情况增加更清楚的提示，区分“没有刺激表”“没有窗口匹配”“响应为空”。

## Future

- 将 10-16 作为进阶分析入口接入 GUI：
  - population features
  - similarity
  - clustering
  - dimensionality reduction
  - cross-trial summary
  - report
- GUI 中可增加 analysis result viewer：
  - 当前 ROI trace / event raster
  - stimulus response heatmap
  - preferred angle / OSI 表
  - trial-level summary

## 今日结论

ROI 后最基础的自动分析链路已经整理为 `basic-analysis`，并且支持只针对当前 trial 运行。这个结构适合作为下一步 GUI 中“运行基础分析”按钮的后端入口。

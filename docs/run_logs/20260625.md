# 2026-06-25 Run Log

## README 重写：补全 pipeline 工作逻辑与具体方法说明

### 修改目的

- 为组会讲解准备一版更适合口头说明的 README。
- 把原先偏命令说明式的文档，改成能直接回答“这个 pipeline 为什么这样设计、每一步如何衔接、具体方法是什么”的版本。

### 本次重点

- 重写了 `README.md` 的主体结构。
- 明确写出三段式主线：
  - `premanual`
  - `manual`
  - `postmanual`
- 把每一步都按以下逻辑说明：
  - 输入
  - 方法
  - 输出
  - 设计原因

### README 新增的关键内容

- 增加了 pipeline 的整体目标说明：
  - 从原始 `.oir` 到可追溯中间结果
  - 到人工校对后的 final ROI
  - 到单 trial 分析
  - 到跨 trial 汇总和 HTML 报告
- 增加了总览流程图，强调：
  - 自动前处理
  - 人工 ROI 定稿
  - 自动后分析
- 增加了“为什么分成 `premanual / manual / postmanual`”的解释。
- 增加了对以下关键设计选择的中文说明：
  - 自动 ROI 只作为候选，不直接当最终结论
  - final trace 从 `03_motion_correct` 重新提取
  - 刺激时间结合 analog 通道与外部刺激记录
  - AoLP tuning 按 `180°` 周期处理
  - 群体分析默认以 stimulus-slice feature 为主

### 方法说明补充

- 对 `00–18` 每一步都补了“具体方法”层面的描述，而不只是列步骤名。
- 特别补清了这些关键步骤：
  - `02`：stim trace 恢复与刺激记录对齐
  - `03`：CaImAn motion correction
  - `05`：suite2p / Cellpose 作为候选 ROI 生成层
  - `06`：从 corrected movie 重新提取 `F/Fneu/dF/F`
  - `08`：构建 peri-stimulus tensor 并计算响应指标
  - `09`：AoLP tuning 的 axial / `180°` 逻辑
  - `12`：stimulus-slice robust normalization 与 evoked 判定
  - `17`：跨 trial 汇总表的意义
  - `18`：HTML 报告作为展示层而不是新分析层

### 影响

- 这次修改主要影响文档表达，不改变代码逻辑和输出格式。
- README 现在更适合：
  - 组会介绍
  - 新人理解 pipeline
  - 后续继续扩写方法学说明

### 修改文件

- `README.md`

### 验证

- 本次没有运行数据处理步骤。
- 只进行了文档层面的结构重写与内容核对。
- 方法描述依据当前 `run_pipeline.py` 和相关步骤脚本、现有命令文档与 GUI 文档整理。

### 后续可做

- 如果后面要专门做组会版文稿，可以再从 README 提炼一页“5 分钟讲清 pipeline”的超短版提纲。
- 如果要写 methods section，可以在当前 README 的基础上继续拆成：
  - 数据整理
  - ROI 策略
  - stimulus response
  - AoLP tuning
  - population analysis

## 17_cross_trial_summary NaN 容错修复

### 背景

- 工作站上一次从 `07` 跑到 `18` 的全链条在 `2026-06-25 01:11:30` 停在 `17_cross_trial_summary.py`。
- `07–16` 已全部成功，失败点是：
  - `build_qc_table()` 中把 `hierarchical_clustering_summary.csv` 里的 `n_clusters=NaN` 直接做 `int(...)`
  - 报错：`ValueError: cannot convert float NaN to integer`

### 处理

- 在 `current/analysis/17_cross_trial_summary.py` 里新增了 `first_numeric_value()` helper。
- 所有跨 trial QC 汇总里需要从 summary 表拿首个数值的位置，都改成：
  - 先 `to_numeric(..., errors="coerce")`
  - 再 `dropna()`
  - 如果为空则返回 `NaN`
  - 只有拿到有效值时才做 `int` / `float` 转换
- 这次一起覆盖了：
  - `n_roi`
  - `n_frames`
  - `n_events`
  - `mean_event_rate_hz`
  - `n_stim_events`
  - `n_responsive_roi`
  - `n_slice_responsive_roi`
  - `n_evoked_slices`
  - `n_angle_selective_roi`
  - `n_hierarchical_clusters`
  - `n_leiden_communities`

### 结果

- 本地 `python3 -m py_compile current/analysis/17_cross_trial_summary.py` 已通过。
- 这属于小范围健壮性修复，不改动分析判定逻辑，只避免 summary 表里存在空值时整个 `17` 崩掉。
- 下一步是同步工作站，并从 `17–18` 继续跑完。

## 导出 polarized test trial 原始/analog 同步视频

### 修改目的

- 快速生成一个可直接查看的 `test` trial 视频。
- 用同一时间轴同时展示：
  - 左侧原始记录 stack
  - 右侧 stim analog stack
  - 下方刺激线
  - 最底部播放进度条
- 在刺激期间直接标出当前偏振角（AoLP）。

### 处理范围

- 只使用现成的 `test_dataset` step-01 / step-02 输出。
- 没有重跑任何分析步骤，没有改动现有 pipeline 主逻辑。

### 新增工具

- 新增 `current/tools/export_trial_sync_video.py`

### 工具逻辑

- 自动读取：
  - `01_oir_to_tif/<trial_id>/<trial_id>_Max_Proj.tif`
  - `01_oir_to_tif/<trial_id>/<trial_id>_Stim_Analog.tif`
  - `01_oir_to_tif/<trial_id>/<trial_id>_metadata.json`
  - `02_stim_map/<trial_id>/<trial_id>_stim_events.csv`
  - `02_stim_map/<trial_id>/<trial_id>_brightness_trace.csv`
- 从 metadata 读取真实采样帧率。
- 从 `stim_events.csv` 读取每段刺激的 `start/end/pol_angle`。
- 导出 mp4 时把两路 stack 做并排同步显示，并在刺激线面板显示当前 AoLP 与播放进度。

### 本次导出目标

- 目标 trial：`20260428_Euprymna_retina_25x_0001`
- 选择原因：
  - 位于 `test_dataset`
  - `stim_events.csv` 中有完整 `pol_angle`
  - 是带 polarized 刺激的 trial

### 验证

- 这次验证方式是小样本真实数据导出，不是全数据集运行。
- 导出前已确认：
  - 原始 stack 与 analog stack 都存在
  - 两者帧数一致，均为 `1100` 帧
  - `stim_events.csv` 存在 `pol_angle`
  - `metadata.json` 可读出 `fps`

### 输出

- 默认输出目录：
  - `test_dataset/18_reports/trial_sync_videos/`
- 本次实际输出：
  - `test_dataset/18_reports/trial_sync_videos/20260428_Euprymna_retina_25x_0001_raw_analog_sync.mp4`

### 后续注意

- 这是展示/检查工具，不替代 step-02 的诊断图和正式分析图。
- 如果后面觉得这个视频样式合适，可以再补可选参数，例如：
  - 只截取某段刺激窗口
  - 放大某个 ROI 区域
  - 叠加 trial id / 刺激编号 / frame id

## README 补充：step 06 基线/背景校正的数学说明

### 修改目的

- 用户明确要求把前面讨论过的“校正基线”写清楚，尤其要区分：
  - 局部 neuropil 扣除
  - 全局 common-mode artifact 回归
  - `F0` 估计与 `ΔF/F` 归一化
- 避免 README 里把这些不同层次都混成一句含糊的“baseline correction”。

### 本次补充

- 重写了 `README.md` 中 step `06` 的“方法”段落。
- 增加了局部 neuropil subtraction 的符号定义：
  - `F_i(t)`
  - `Fneu_i(t)`
  - `alpha`
  - `F_local_i(t) = F_i(t) - alpha * Fneu_i(t)`
- 增加了 non-ROI PCA artifact regression 的数学说明：
  - non-ROI tile 背景 trace `b_k(t)`
  - 全局 common-mode `g(t)`
  - PCA 背景分量 `p_1(t)...p_n(t)`
  - nuisance regression 的线性模型与回归后 `F_clean`
- 增加了 `F0` 几种模式的含义说明：
  - `percentile`
  - `stim-baseline`
  - `rolling-percentile`
  - `rolling-median`
- 明确写出最终归一化公式：

```text
dF/F_i(t) = (F_clean_i(t) - F0_i(t)) / max(|F0_i(t)|, eps)
```

### 影响

- 本次修改只影响 README 和方法学表达，不改动 step `06` 代码逻辑。
- 文档现在更适合直接用于组会口头解释，也更方便后续展开 methods section。

### 修改文件

- `README.md`
- `docs/run_logs/20260625.md`

### 验证

- 这次没有重跑数据分析。
- 依据当前 `current/analysis/06_extract_dff.py` 的实现核对 README 描述，确保公式与代码一致。

## 导出同一 trial 的 CaImAn / spatial high-pass 对比视频

### 修改目的

- 快速生成可直接查看的 movie 对比视频，用于直观看：
  - `CaImAn motion correction` 前后差异
  - `motion corrected` 与 `spatial high-pass` 的显示差异
- 两条视频都要求：
  - 同一 trial
  - 左右并排
  - `60x` 速度
  - `60 fps`

### 处理范围

- 只使用 `test_dataset` 中现成的 step-01 / step-03 / step-04 movie 和 metadata。
- 没有重跑任何 motion correction 或 high-pass 步骤。

### 新增工具

- 新增 `current/tools/export_movie_comparison_video.py`

### 工具逻辑

- 输入：
  - 任意两个同 shape 的 movie TIFF
  - 一个 metadata JSON 读取真实采样帧率
- 输出：
  - 左右双窗 mp4
  - 底部带统一进度条
- 支持：
  - `--speed`
  - `--output-fps`
  - `--left-label / --right-label`
- 当前实现按目标倍率抽帧，因此 `60x` 输出不会只是“高帧率慢速写出”，而是实际压缩了内容时间。

### 本次导出目标

- 目标 trial：`20260428_Euprymna_retina_25x_0001`
- 选择原因：
  - `01_oir_to_tif`、`03_motion_correct`、`04_spatial_highpass` 三个 movie 都齐全
  - 三者帧数和尺寸一致，均为 `1100 x 512 x 512`
  - step-01 metadata 可读出真实 `fps`

### 本次输出

- `test_dataset/18_reports/trial_comparison_videos/20260428_Euprymna_retina_25x_0001_raw_vs_motion_corrected_60x_60fps.mp4`
- `test_dataset/18_reports/trial_comparison_videos/20260428_Euprymna_retina_25x_0001_motion_corrected_vs_spatial_highpass_60x_60fps.mp4`
- smoke:
  - `test_dataset/18_reports/trial_comparison_videos/20260428_Euprymna_retina_25x_0001_raw_vs_motion_corrected_60x_60fps_smoke.mp4`
  - `test_dataset/18_reports/trial_comparison_videos/20260428_Euprymna_retina_25x_0001_motion_corrected_vs_spatial_highpass_60x_60fps_smoke.mp4`

### 验证

- 先做小样本 smoke export，再做完整导出。
- 导出前已确认：
  - 原始 / corrected / high-pass movie 均存在
  - 三者 shape 一致
  - metadata 可读出真实采样帧率
- 完整导出后已确认：
  - 两条正式版均为 `h264`
  - 分辨率均为 `1440 x 816`
  - 帧率均为 `60 fps`
  - 时长均约 `11.03 s`
  - 帧数均为 `662`

### 后续注意

- 这个工具是展示工具，不改变任何分析结果。
- 如果后面觉得有用，可以继续补：
  - 同步叠加位移轨迹
  - 只截取局部 ROI 区域
  - 高亮边缘或差分图

## 统一切换到 20260428_Euprymna_retina2_25x_0002 导出展示视频

### 修改目的

- 将本地展示视频统一切换到用户指定 trial：
  - `20260428_Euprymna_retina2_25x_0002`
- 新增一个“单独的 motion corrected movie”导出结果。

### 处理范围

- 只使用 `test_dataset` 中该 trial 的现成输出：
  - `01_oir_to_tif`
  - `02_stim_map`
  - `03_motion_correct`
  - `04_spatial_highpass`
- 没有重跑 motion correction、stim map 或 spatial high-pass。

### 新增工具

- 新增 `current/tools/export_single_movie_video.py`

### 修改文件

- `current/tools/export_single_movie_video.py`
  - 新增单窗口 movie 导出工具。
  - 支持真实采样帧率读取、目标倍率抽帧、底部进度条和 `60 fps` 输出。

### 本次输出

- 单独 movie：
  - `test_dataset/18_reports/trial_single_movie_videos/20260428_Euprymna_retina2_25x_0002_motion_corrected_60x_60fps.mp4`
- 原始记录 vs analog：
  - `test_dataset/18_reports/trial_sync_videos/20260428_Euprymna_retina2_25x_0002_raw_analog_sync_60x_60fps.mp4`
- 原始记录 vs motion corrected：
  - `test_dataset/18_reports/trial_comparison_videos/20260428_Euprymna_retina2_25x_0002_raw_vs_motion_corrected_60x_60fps.mp4`
- motion corrected vs spatial high-pass：
  - `test_dataset/18_reports/trial_comparison_videos/20260428_Euprymna_retina2_25x_0002_motion_corrected_vs_spatial_highpass_60x_60fps.mp4`

### 验证

- 先对单独 movie 做了最小 smoke test：
  - `test_dataset/18_reports/trial_single_movie_videos/20260428_Euprymna_retina2_25x_0002_motion_corrected_smoke.mp4`
- 完整导出后用 `ffprobe` 核对四个正式输出：
  - 都是 `60 fps`
  - 时长都约 `11.03 s`
  - `raw vs analog` 分辨率为 `1440 x 960`
  - 两个双窗 comparison 分辨率为 `1440 x 816`
  - 单独 motion corrected 分辨率为 `864 x 816`

### 后续注意

- 本次只是展示导出，不改变下游分析结果。
- 当前导出编码为 `mpeg4/mp4v`，原因是本地与工作站的 `imageio`/PyAV 后端兼容性不一致，已统一改用 `OpenCV VideoWriter` 保证稳定导出。

## 工作站 06-18 同一 trial 结果整理成 PPT 并拉回本地

### 修改目的

- 从工作站已有结果中，围绕同一个 trial 选择 `06` 到 `18` 的代表性结果，整理成可直接用于汇报的 PPT。
- 目标不是做新的分析，而是把已经存在的工作站输出按“同一 trial 主线”串起来。

### 选择策略

- 固定 trial：
  - `20260621_Euprymna_retina_25x_Cl50_0001`
- 选择理由：
  - `12_stimulus_slice_features` 已有完整 heatmap；
  - `14`、`15`、`16` 都有可直接展示的群体结构图；
  - `18_reports` 已有 per-trial HTML report；
  - 同时也能如实展示当前限制：`09_angle_tuning` 为空表。

### 使用输入

- 工作站 pipeline 根目录：
  - `/mnt/50d357b2-473b-4533-8c74-1db99704876a/yifeiding/calcium_imaging/2026_olympus`
- 本次从工作站拉回本地的代表图/文件包括：
  - `08_stim_response/*_stim_response_heatmap.png`
  - `08_stim_response/*_psth_examples.png`
  - `11_population_features/*_feature_correlation_heatmap.png`
  - `12_stimulus_slice_features/*_stim_slice_response_heatmap.png`
  - `13_population_similarity/*_similarity_heatmap.png`
  - `14_hierarchical_clustering/*_clustered_trace_heatmap_normalized_dff.png`
  - `14_hierarchical_clustering/*_cluster_mean_traces.png`
  - `15_leiden/*_leiden_embedding.png`
  - `15_leiden/*_leiden_roi_spatial_map.png`
  - `16_dimensionality_reduction/*_pca_plot.png`
  - `17_cross_trial_summary/cross_trial_responsive_fraction.png`
  - `17_cross_trial_summary/cross_trial_roi_count.png`
  - `18_reports/20260621/20260621_Euprymna_retina_25x_Cl50_0001_report.html`
- 本地补充使用但未重跑分析的已有文件：
  - `06_dff/*_dff_heatmap.png`
  - `06_dff/*_example_traces.png`
  - `10_trace_plots/*_trace_overview.png`
  - `16_reports/20260621/_assets/*_mean_image.png`

### 关键摘要

- 该 trial `06` 结果中的 ROI 数为 `50`。
- `08` 的 `response_type` 统计为：
  - `48` 个 `nonresponsive`
  - `1` 个 `mixed`
  - `1` 个 `offset`
- `09_angle_tuning_summary.csv` 为 `0` 行，当前不能讲成已有 AoLP 调谐结果。
- `17_cross_trial_summary/top_responsive_rois.csv` 中，该 trial 仍命中 `20` 条 top responsive ROI 记录。
- `trial_context_summary.csv` 里该 trial 的上下文为：
  - `chloride_50pct_replacement`
  - `reduced_chloride = yes`
  - `planned_stimulus_mode = sustain`
  - `measured_stimulus_mode = sustain`
  - `measured_has_AoLP = no`

### 本次输出

- 本地素材整理目录：
  - `/Users/dingyifei/Documents/calcium-imaging-pipeline-new/meeting_selection_20260625_step06_18`
- 最终 PPT：
  - `/Users/dingyifei/Documents/calcium-imaging-pipeline-new/outputs/20260625_step06_18_same_trial_workstation.pptx`
- 同步生成的检查文件：
  - `/Users/dingyifei/Documents/calcium-imaging-pipeline-new/outputs/20260625_step06_18_same_trial_workstation.pptx.inspect.ndjson`

### 验证

- 只使用现有结果文件，没有重跑 `06-18` 分析链。
- PPT 生成前后均检查了：
  - 工作站目标文件存在；
  - 本地拉回文件完整；
  - `09` 的空表状态真实保留；
  - PPT 逐页预览 PNG 已导出并人工检查版式。

### 后续注意

- 这份 PPT 适合讲“流程链条已通、部分结构可读”，不适合把该 trial 讲成强阳性响应例子。
- 如果后面需要更强的单 trial 正例，可按同一模板替换成 `20260411_Euprymna_retina_xx_0003` 或 `0009`。
- 本次没有自动同步回工作站；只是从工作站拉文件到本地并整理汇报材料。

## 10:09 单 trial 分析结果下载补记

### 目标

- 从工作站拉回 `20260428_Euprymna_retina2_25x_0002` 的现成分析结果，供本地继续整理。
- 明确排除原始视频和 `05` 及之前的重型原始/中间大文件。

### 实际下载范围

- 保留并下载：
  - `02_stim_map`
  - `05_cellpose_roi_segmentation`
  - `05_suite2p_roi_detection` 中轻量结果文件，排除其 `suite2p/` 与 `benchmark/` 大目录
  - `05e_roi_manual_curation`
  - `06_dff` 到 `16_dimensionality_reduction`
  - `18_reports` 中该 trial 的 HTML 报告
- 未下载：
  - `00_original_files`
  - `01_oir_to_tif`
  - `03_motion_correct`
  - `04_spatial_highpass`
  - `05` 及之前的视频样大文件和重型中间结果
  - `17_cross_trial_summary`，因为它是跨 trial 汇总，不是该 trial 独有结果

### 本地落点与结果

- 下载目录：
  - `/Users/dingyifei/Documents/calcium-imaging-pipeline-new/workstation_trial_downloads/20260428_Euprymna_retina2_25x_0002_analysis_only`
- 本地体积：
  - 约 `317M`
- 目录核对完成，已包含 `06-16` 全部 trial 级分析结果及 `18` 报告页。
